# Application
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Database — not wired in Pass 1; will be connected in Pass 2
DATABASE_URL=postgresql://rmias:rmias@db:5432/rmias
POSTGRES_DB=rmias
POSTGRES_USER=rmias
POSTGRES_PASSWORD=rmias

# Raw payload storage — not wired in Pass 1; will be used from Pass 5
RAW_PAYLOAD_STORAGE_PATH=/data/raw_payloads
